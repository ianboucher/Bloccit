<h1><%= @post.title %></h1>

 <div class="row">
   <div class="col-md-8">
     <p><%= @post.body %></p>
   </div>
   <div class="col-md-4">
     <!-- As with the social btn, link_to is used to add a link to a button, but
      in this case edit_post_path is used to link to the 'edit' page for that
      post i.e /posts/@post.id/edit-->
     <%= link_to "Edit", edit_post_path(@post), class: 'btn btn-success' %>
     <!--There is no helper method listed in the routes for 'post DELETE' so the
     HTTP verb 'delete' is requred alongside 'method:'. The confimation message
     is then specified via the 'data' hash-->
     <%= link_to "Delete Post", @post, method: :delete, class: 'btn btn-danger',
        data: { confirm: "Are you sure you want to delete this post?" } %>
   </div>
 </div>
